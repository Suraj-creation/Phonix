node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
phoenix.zip

# Queued Google Sheets pushes — contains student names, emails and phone
# numbers, so it must never be committed.
sheets_sync_failed.jsonl
